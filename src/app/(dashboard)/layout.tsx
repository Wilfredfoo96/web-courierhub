import DashboardlLayout from "@/components/layout/dashboard-layout";

export default function Dashboard({ children }: { children: React.ReactNode }) {
  return <DashboardlLayout>{children}</DashboardlLayout>;
}
